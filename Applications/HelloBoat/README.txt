
This project was created in eclipse as a "WebObjects Application", so
eclipse should be able to build it.

But then do:

$ mv HelloBoatExe build/HelloBoat.woa/
$ cd build/HelloBoat.woa
$ ./HelloBoatExe

And then you will see the use of the replaced classes in the JavaWebObjects
framework.

